{
  "title": "Facebook Data Fetch Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "urls": {
      "title": "Facebook URLs",
      "type": "string",
      "description": "Enter Facebook URLs to fetch data from (one per line)",
      "editor": "textarea",
      "prefill": "https://www.facebook.com/example-page\nhttps://www.facebook.com/another-page"
    },
    "maxItems": {
      "title": "Maximum items to fetch",
      "type": "integer",
      "description": "Maximum number of items to fetch per URL",
      "minimum": 1,
      "maximum": 1000,
      "default": 100
    },
    "waitForSelector": {
      "title": "Wait for selector",
      "type": "string",
      "description": "CSS selector to wait for before scraping (optional)",
      "editor": "textfield"
    }
  },
  "required": ["urls"]
}
