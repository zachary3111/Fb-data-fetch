{
  "title": "Facebook Data Fetch Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "urls": {
      "title": "Facebook URLs",
      "type": "array",
      "description": "List of Facebook URLs to fetch data from",
      "editor": "stringArray",
      "items": {
        "type": "string",
        "title": "URL"
      },
      "minItems": 1
    },
    "maxItems": {
      "title": "Maximum items to fetch",
      "type": "integer",
      "description": "Maximum number of items to fetch per URL",
      "minimum": 1,
      "maximum": 1000,
      "default": 100
    }
  },
  "required": ["urls"]
}
