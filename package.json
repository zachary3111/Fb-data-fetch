{
  "name": "fb-posts-and-dates-merged",
  "version": "0.3.2",
  "private": true,
  "type": "module",
  "description": "Apify actor: scrape Facebook post details or post dates (with OCR fallback)",
  "scripts": {
    "start": "node src/main.js",
    "test:smoke": "node scripts/smoke-parse-dates.mjs",
    "test:dates": "node scripts/test-parse-dates.mjs",
    "test:edges": "node scripts/test-parse-dates-edges.mjs",
    "test": "npm run test:smoke && npm run test:dates && npm run test:edges"
  },
  "dependencies": {
    "apify": "^3.1.10",
    "playwright": "^1.47.2",
    "tesseract.js": "^5.1.0"
  }
}